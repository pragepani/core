# Requirements 📋

Contributor-side tooling that maintains the requirements directory at `docs/requirements/`: archive completed requirements into the `## Archive` index in the directory README and delete the per-requirement files.
