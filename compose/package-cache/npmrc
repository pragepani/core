# npm → cache frontend via registry.npmjs.org (mapped by extra_hosts);
# not `package-cache`, which coredns can't resolve. strict-ssl=false
# accepts the frontend's self-signed cert.
registry=https://registry.npmjs.org/
strict-ssl=false
