---
# nocheck: playwright-service-flag — static-asset provider, no Playwright surface
asset:
  enabled: "{{ 'web-svc-asset' in group_names }}"
  shared: "{{ 'web-svc-asset' in group_names }}"
sso:
  enabled: "{{ 'web-app-keycloak' in group_names }}"
  shared: "{{ 'web-app-keycloak' in group_names }}"
  flavor: oidc
# nocheck: dynamic-flag
ldap:
  enabled: false
  shared: false
logout:
  enabled: "{{ 'web-svc-logout' in group_names }}"
  shared: "{{ 'web-svc-logout' in group_names }}"
dashboard:
  enabled: "{{ 'web-app-dashboard' in group_names }}"
  shared: "{{ 'web-app-dashboard' in group_names }}"
matomo:
  enabled: "{{ 'web-app-matomo' in group_names }}"
  shared: "{{ 'web-app-matomo' in group_names }}"
email:
  enabled: "{{ 'web-app-mailu' in group_names }}"
  shared: "{{ 'web-app-mailu' in group_names }}"
# nocheck: playwright-service-flag — DB engine, covered by role-local integration tests
postgres:
  enabled: true # nocheck: dynamic-flag
  shared: "{{ 'svc-db-postgres' in group_names }}"
  extensions:
    - vector
# nocheck: shared
# nocheck: dynamic-flag
# nocheck: playwright-service-flag — DB engine, covered by role-local integration tests
redis:
  backup:
    disabled: true
  enabled: true
# nocheck: playwright-service-flag — self-provider
discourse:
  # `shared: true` is required so the service registry recognises this
  # role as the provider of the `discourse` service. Other roles
  # declare `services.discourse.enabled: ...` (e.g.
  # web-app-wordpress' wp-discourse plugin gate); without this flag
  # the variant-aware deploy planner cannot resolve those declarations
  # to web-app-discourse, and the consumer ends up trying to
  # `docker exec discourse` against a container that was never pulled.
  shared: true
  enabled: true
  name: discourse
  ref: main
  min_storage: 15GB
  repository: https://github.com/discourse/discourse_docker.git
  plugins:
    docker_manager:
      enabled: true
    discourse-activity-pub:
      enabled: true
    discourse-akismet:
      enabled: true
    discourse-ldap-auth:
      enabled: "{{ lookup('config', 'web-app-discourse', 'services.ldap.enabled') | bool }}"
      repository: https://github.com/jonmbake/discourse-ldap-auth.git
      ref: master
  ports:
    local:
      http: 8020
  lifecycle: beta
  cpus: "1.0"
  mem_reservation: 1g
  mem_limit: 2g
  pids_limit: 1024
  local_login_enabled: "{{ not (lookup('config', 'web-app-discourse', 'services.sso.enabled') | bool) or (lookup('config', 'web-app-discourse', 'services.ldap.enabled') | bool) }}"
# nocheck: playwright-service-flag — css consumer, no Playwright surface (provider: web-svc-css)
css:
  enabled: "{{ 'web-svc-css' in group_names }}"
  shared: "{{ 'web-svc-css' in group_names }}"
prometheus:
  enabled: "{{ 'web-app-prometheus' in group_names }}"
  shared: "{{ 'web-app-prometheus' in group_names }}"
