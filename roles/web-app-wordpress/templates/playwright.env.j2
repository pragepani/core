ADMIN_PASSWORD={{ lookup('users', 'administrator').password | dotenv_quote }}

ADMIN_USERNAME={{ lookup('users', 'administrator').username | dotenv_quote }}

APP_BASE_URL={{ lookup('tls', application_id, 'url.base') | dotenv_quote }}

BIBER_PASSWORD={{ lookup('users', 'biber').password | dotenv_quote }}

BIBER_USERNAME={{ lookup('users', 'biber').username | dotenv_quote }}

CANONICAL_DOMAIN={{ lookup('domain', application_id) | dotenv_quote }}

DISCOURSE_API_KEY={{ (vault_discourse_api_key | default('')) | dotenv_quote }}

DISCOURSE_API_USERNAME={{ 'system' | dotenv_quote }}

# --- WP-Discourse round-trip ----------------------------------------------
DISCOURSE_BASE_URL={{ (lookup('tls', 'web-app-discourse', 'url.base') | dotenv_quote) if (lookup('config', application_id, 'services.discourse.enabled') | bool) else '""' }}

# Discourse keys off services.discourse.enabled; raw group_names is unreliable because matrix-deploy keeps the dep across all variants.
DISCOURSE_SERVICE_ENABLED={{ lookup('config', application_id, 'services.discourse.enabled') | string | lower }}

KEYCLOAK_BASE_URL={{ lookup('tls', 'web-app-keycloak', 'url.base') | dotenv_quote }}

KEYCLOAK_REALM_NAME={{ OIDC.CLIENT.REALM | dotenv_quote }}

LDAP_SERVICE_ENABLED={{ lookup('service', 'ldap').required | string | lower }}

# Service gate flags rendered for closure.
LOGOUT_SERVICE_ENABLED={{ lookup('config', application_id, 'services.logout.enabled') | string | lower }}

# Service gate flags. Values are literal "true"/"false"; `tests/service-gating.js` reads them and hard-fails on unknown service names.
MATOMO_BASE_URL={{ lookup('tls', 'web-app-matomo', 'url.base') | dotenv_quote }}

MATOMO_SERVICE_ENABLED={{ lookup('config', application_id, 'services.matomo.enabled') | string | lower }}

OIDC_ISSUER_URL={{ OIDC.CLIENT.ISSUER_URL | dotenv_quote }}

PERSONA_ADMINISTRATOR_BLOCKED=true

PERSONA_BIBER_BLOCKED=true

RBAC_GROUP_PATH_PREFIX={{ ('/' ~ (RBAC.GROUP.NAME | trim('/')) ~ '/' ~ application_id ~ '/') | dotenv_quote }}

SSO_SERVICE_ENABLED={{ lookup('service', 'sso').required | string | lower }}

SUPER_ADMIN_PASSWORD={{ lookup('config', 'web-app-keycloak', 'credentials.administrator_password') | dotenv_quote }}

SUPER_ADMIN_USERNAME={{ lookup('config', 'web-app-keycloak', 'services.keycloak.accounts.system.username') | dotenv_quote }}

WORDPRESS_BASE_URL={{ lookup('tls', application_id, 'url.base') | dotenv_quote }}

# WordPress Multisite toggle (true = multi-domain Multisite, false = Single-Site); source: services.wordpress.multisite.enabled.
WORDPRESS_MULTISITE_ENABLED={{ lookup('config', application_id, 'services.wordpress.multisite.enabled') | string | lower }}

