---
sso:
  enabled: "{{ 'web-app-keycloak' in group_names }}"
  shared: "{{ 'web-app-keycloak' in group_names }}"
  flavor: oidc
logout:
  enabled: "{{ 'web-svc-logout' in group_names }}"
  shared: "{{ 'web-svc-logout' in group_names }}"
dashboard:
  enabled: "{{ 'web-app-dashboard' in group_names }}"
  shared: "{{ 'web-app-dashboard' in group_names }}"
matomo:
  enabled: "{{ 'web-app-matomo' in group_names }}"
  shared: "{{ 'web-app-matomo' in group_names }}"
# nocheck: playwright-service-flag — DB engine, covered by role-local integration tests
mariadb:
  enabled: true # nocheck: dynamic-flag
  shared: "{{ 'svc-db-mariadb' in group_names }}"
discourse:
  enabled: "{{ 'web-app-discourse' in group_names }}"
  shared: "{{ 'web-app-discourse' in group_names }}"
wordpress:
  version: 7.0.0
  image: wordpress
  name: wordpress
  min_storage: 8GB
  backup:
    no_stop_required: true
  cpus: 1
  mem_reservation: 0.5g
  mem_limit: 1.5g
  pids_limit: 512
  title: Blog by {{ service_provider.platform.titel }}
  max_upload_size: 256M
  multisite:
    enabled: false
  plugins:
    wp-discourse:
      enabled: "{{ lookup('config', 'web-app-wordpress', 'services.discourse.enabled') | bool }}"
    daggerhart-openid-connect-generic:
      enabled: "{{ lookup('config', 'web-app-wordpress', 'services.sso.enabled') | bool }}"
    activitypub:
      enabled: true
  ports:
    local:
      http: 8003
  run_after:
    - web-app-keycloak
  lifecycle: beta
# nocheck: playwright-service-flag — verified by web-svc-css provider spec
css:
  enabled: "{{ 'web-svc-css' in group_names }}"
  shared: "{{ 'web-svc-css' in group_names }}"
prometheus:
  enabled: "{{ 'web-app-prometheus' in group_names }}"
  shared: "{{ 'web-app-prometheus' in group_names }}"
