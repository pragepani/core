- include_tasks: utils/once/flag.yml

- name: "Load docker, db and proxy for {{ application_id }}"
  include_role:
    name: sys-stk-full

- name: "Run Pi-hole setup routines"
  include_tasks: 01_setup.yml
