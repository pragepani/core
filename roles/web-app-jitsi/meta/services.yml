---
sso:
  enabled: "{{ 'web-app-keycloak' in group_names }}"
  shared: "{{ 'web-app-keycloak' in group_names }}"
  flavor: oauth2
  oauth2:
    origin:
      host: application
      port: 80
ldap:
  enabled: "{{ 'svc-db-openldap' in group_names }}"
  shared: "{{ 'svc-db-openldap' in group_names }}"
logout:
  enabled: "{{ 'web-svc-logout' in group_names }}"
  shared: "{{ 'web-svc-logout' in group_names }}"
dashboard:
  enabled: "{{ 'web-app-dashboard' in group_names }}"
  shared: "{{ 'web-app-dashboard' in group_names }}"
matomo:
  enabled: "{{ 'web-app-matomo' in group_names }}"
  shared: "{{ 'web-app-matomo' in group_names }}"
prometheus:
  enabled: "{{ 'web-app-prometheus' in group_names }}"
  shared: "{{ 'web-app-prometheus' in group_names }}"
# nocheck: playwright-service-flag — email not used by Jitsi room flow
email:
  enabled: "{{ 'web-app-mailu' in group_names }}"
  shared: "{{ 'web-app-mailu' in group_names }}"
# nocheck: playwright-service-flag — css consumer, no Playwright surface (provider: web-svc-css)
css:
  enabled: "{{ 'web-svc-css' in group_names }}"
  shared: "{{ 'web-svc-css' in group_names }}"
# nocheck: dynamic-flag — gated on services.sso.enabled, not on group_names
# nocheck: playwright-service-flag — runtime DOM patch, no probe-able surface flag
# Inject the role-local logout button (templates/javascript.js.j2) when SSO is on, so
# Jitsi's React SPA (which has no native sign-out control) still exposes a click target
# that survives the persona contract's no-direct-URL logout rule.
javascript:
  enabled: "{{ lookup('config', 'web-app-jitsi', 'services.sso.enabled', False) | bool }}"
  shared: "{{ lookup('config', 'web-app-jitsi', 'services.sso.enabled', False) | bool }}"
jitsi:
  image: jitsi/web
  version: "stable-9646"
  min_storage: 5GB
  ports:
    local:
      http: 8084
      sso: 16505
    public:
      media: 10000
  run_after:
    - web-app-keycloak
  lifecycle: alpha
  cpus: "2.0"
  mem_reservation: 1g
  mem_limit: 3g
  pids_limit: 2048
# nocheck: playwright-service-flag — internal sub-component (XMPP server, not a user-facing surface)
prosody:
  image: jitsi/prosody
  version: "stable-9646"
  cpus: "1.0"
  mem_reservation: 512m
  mem_limit: 1g
  pids_limit: 1024
# nocheck: playwright-service-flag — internal sub-component (conference focus)
jicofo:
  image: jitsi/jicofo
  version: "stable-9646"
  cpus: "1.0"
  mem_reservation: 512m
  mem_limit: 1g
  pids_limit: 1024
# nocheck: playwright-service-flag — internal sub-component (video bridge, media plane)
jvb:
  image: jitsi/jvb
  version: "stable-9646"
  cpus: "2.0"
  mem_reservation: 1g
  mem_limit: 2g
  pids_limit: 2048
