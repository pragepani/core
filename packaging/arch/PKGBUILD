# Maintainer: Kevin Veen-Birkenbach <info@veen.world>

pkgname=infinito-nexus
pkgver=8.0.1
pkgrel=1
pkgdesc="Meta package for Infinito.Nexus host dependencies (dev + CI)."
arch=('any')
url="https://github.com/infinito-nexus/core"
license=('custom')

depends=(
  'ansible'
  'bash'
  'ca-certificates'
  'curl'
  'dbus'
  'docker'
  'docker-compose'
  'gettext'
  'git'
  'jq'
  'make'
  'openssh'
  'python'
  'python-pip'
  'python-yaml'
  'rsync'
  'sudo'
  'systemd'
  'tar'
)

optdepends=(
  'bind: provides dig for DNS test scripts'
  'shellcheck: shell linting'
  'shfmt: shell formatting'
)

install="${pkgname}.install"
source=()
sha256sums=()

package() {
  install -Dm0644 /dev/null "${pkgdir}/usr/share/doc/${pkgname}/DEPENDENCIES"
}
